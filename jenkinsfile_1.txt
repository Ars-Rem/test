pipeline {
    agent any 
    
    stages {
        stage("Hello, world!") {
            steps {
                echo "Hello, world!"
            }
        }
        
        stage("compile") {
            steps {
                echo "Start compile"
                echo "Building..."
                sh "cd /var/lib/jenkins/workspace/test"
                sh "npm run build"
                echo "Stop.."
              }
            }
            
        stage("send") {
           steps {
                echo "Start send"
                
                
                
                echo "Stop.."
            }
        }   
    }
 }
